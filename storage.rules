rules_version = '2';

service firebase.storage {
  // This rule applies only to the 'user-profile-pages' bucket
  match /b/user-profile-pages/o {
    // This matches any file at any path within that bucket
    match /{allPaths=**} {
      allow read;
      allow write: if false; // Explicitly deny writes for security
    }
  }
}